/** The assistant lives in shared/assistant.ts so the app and the site answer identically. */
export { SYSTEM_PROMPT, localAnswer, fallbackAnswer, greeting, CHIPS, buildFacts, type Reply } from "@f7/content";
